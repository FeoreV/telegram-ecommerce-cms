# Vault Configuration
USE_VAULT=true
VAULT_ADDR=http://82.147.84.78:8200
VAULT_ROLE_ID=your-role-id-from-vault-init
VAULT_SECRET_ID=your-secret-id-from-vault-init
VAULT_NAMESPACE=

# Vault Transit Engine (for encryption)
VAULT_TRANSIT_KEY=telegram-ecommerce-key

# Fallback configuration (if Vault is unavailable)
# These will be ignored if USE_VAULT=true
JWT_SECRET=fallback-jwt-secret-at-least-32-chars
JWT_REFRESH_SECRET=fallback-refresh-secret-at-least-32-chars
DATABASE_URL=file:./dev.db
TELEGRAM_BOT_TOKEN=fallback-bot-token
